# Synced Frontend Copy

This folder contains a synced fallback copy of the Smart Assignment Maker frontend.

The primary application folders are:

- `../frontend` for the React app
- `../backend` for the FastAPI service

Prefer deploying from the primary folders unless your host is already configured for this nested copy.
